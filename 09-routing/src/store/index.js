import { createStore } from 'vuex';

const store = {
  state: {
  },

  getters: {},

  mutations: {
  },

  actions: {
  },

  strict: process.env.NODE_ENV !== 'production',
};

export default createStore(store);
